export enum NODE_ENV {
  DEVELOPMENT = 'development',
  PRODUCTION = 'production'
}

export enum APP_CONSTANT {
  BACKEND_MICROSERVICES = 'backend-microservices',
  BACKEND_CLIENT = 'backend',
  VA_KAFKA = 'va-kafka',
  VA_CLIENT = 'va-client',
  VA_CONSUMER = 'va-consumer'
}
